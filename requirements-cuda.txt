# Cloud-GPU requirements (Vast.ai / CUDA 12.1)
# Use in place of `pip install -e .[support-aug]` on a CUDA host.
# torch-directml is intentionally NOT installed — _select_device() auto-detects CUDA.
#
# Install order:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.4.1
#   pip install -r requirements-cuda.txt
#   pip install -e . --no-deps    # install the football_betting package itself

# ── core runtime (mirrors [project].dependencies, minus heavy unused libs) ──
pandas>=2.2
numpy>=1.26,<2.0
scikit-learn>=1.4
scipy>=1.12
click>=8.1
rich>=13.7
pydantic>=2.6
joblib>=1.3
pyarrow>=15.0
requests>=2.31

# ── transformer stack (support-aug extras) ──
transformers>=4.40,<4.50
sentencepiece>=0.2
sacremoses>=0.1
tokenizers>=0.19
safetensors>=0.4
accelerate>=0.30

# ── ONNX export (optional but cheap) ──
onnx>=1.15
onnxruntime>=1.17

# Not installed on purpose:
#   torch-directml   — Windows/AMD only; conflicts with CUDA wheels
#   catboost         — tabular model, not needed for support-classifier training
#   matplotlib       — not used during training
#   fastapi/uvicorn  — API layer, not needed on training host
